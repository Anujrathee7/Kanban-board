{
  "name": "project_advanced_web",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "preinstall": "cd kanban-board-backend && npm install",
    "install": "npm install concurrently && cd kanban-board && npm install",
    "dev:frontend": "npm run dev --prefix kanban-board",
    "dev:backend": "npm run dev --prefix kanban-board-backend",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}
